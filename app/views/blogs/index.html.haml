= render partial: 'blog', collection: @blogs

= form_for @blog do |f|
  .form
    = f.text_field :content, {placeholder: "投稿する", class: "form-control form-control-lg"}
    = f.file_field :image
    = f.submit "投稿", {class: "btn btn-primary"}
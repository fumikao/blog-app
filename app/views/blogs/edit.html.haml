%h2 編集
= form_for @blog do |f|
  .form
    = f.text_field :content, {placeholder: "#{@blog.content}", class: "form-control form-control-lg"}
    = f.file_field :image, {placeholder: "#{@blog.image}"}
    = f.submit "編集", {class: "btn btn-primary"}